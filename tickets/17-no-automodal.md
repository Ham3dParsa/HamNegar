# Ticket 17 — حذف مودال خودکار اول‌اجرا (علت: دکمه‌های پالایش «مرده»)

## مشکل (تأییدشده با Playwright، پروفایل تازه)
- `js/app.js:444` در `loadSettings()`: اگر هیچ کلیدی نباشد، `openModal()` صدا زده می‌شود.
- `#settings-modal` فول‌اسکرین (`css/app.css:170`، `z-index:1000`) کل صفحه را می‌پوشاند؛ کلیک روی
  `stage-simple/advanced/grammar` به اورلی می‌خورد → از نظر کاربر «کاری نمی‌کند».
- با بستن مودال (Esc)، دکمه‌ها سالم‌اند و مسیر خطای بی‌کلیدی درست کار می‌کند
  (`❌ پالایش ساده: مدل متنی در زنجیره نیست` در لاگ). پس فقط همین auto-open مقصر است.

## تصمیم مالک
مودال خودکار باز **نشود**؛ فقط هشدار + راهنما.

## تغییر (فقط همین Seam — سیم‌کشی مودال/استارتاپ)
1. `js/app.js:444`: حذف صدای `openModal()` در شاخه بی‌کلیدی؛ نگه‌داشتن
   `Logger.setStatus('کلید تنظیم نشده — ⚙️ را بزن','warn')` (همان هشدار + راهنما).
2. `js/app.js:1298`: به‌روزرسانی کامنت (`openModal() assigns it on fresh profiles` دیگر درست نیست).
3. دست‌نخورده می‌ماند: `js/app.js:1630` (بازشدن مودال هنگام ضبط واقعی بدون کلید — متنی و درست است)،
   کل منطق `openModal/closeModal`، focus-trap، Esc.

## قبول
- پروفایل تازه (localStorage خالی): `getComputedStyle(#settings-modal).display === 'none'`، استاتوس warn دیده می‌شود.
- با کلید موجود: رفتار قبلی (`آماده به کار`).
- Playwright: بدون `pageerror`؛ کلیک `stage-simple` با مودال بسته به لاگ خطای بی‌کلیدی می‌رسد (نه سکوت).
- `git diff --check` تمیز + gate بازبین.
