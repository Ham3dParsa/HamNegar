# Ticket 09 — سیال‌سازی سراسری + اسکرول‌بار مدرن (پورت fluid دموی کامل)

## Question
حذف اسکرول‌بار کافی نیست؛ چیدمان باید در همه بخش‌ها در هر عرضی پویا باشد. چطور؟ (پورت `fluid-*` دموی کامل: ۵ عرض تمیز)

## Scope — Seam: css/app.css (global)
- فقط `css/app.css` (یک بلوک الحاقی در انتها)
- `index.html`, `js/**` را دست نزن (اورراید inline-style لاگ فقط با سلکتور صفتی، بدون ویرایش HTML)
- پیش‌نیاز: Ticket 08 (ساختار نهایی مدل‌ها)

## Spec
- `clamp()/minmax(0,1fr)/flex` همه‌جا؛ بدون عرض ثابت روی کانتینر؛ ellipsis متن‌های پویا (وضعیت، شمارش، scope، بج‌ها)؛ دکمه‌ها باریک ثابت؛ چیپ‌ها wrap.
- ریل‌ها (`stage-row` و مشابه): اسکرول-x با اسکرول‌بار مخفی در همه عرض‌ها؛ بقیه اسکرول‌شونده‌ها: نازک مدرن (≤8px، ترک شفاف، thumb خنثی؛ فایرفاکس thin).
- حفظ فیکس منوی تنظیمات مراحل (بازشو زیر نوار فقط موقع open)؛ گرید wave در ≤640px تک‌ستونه؛ `.scope` در ≤390px مخفی.
- صفحه: بدون اسکرول افقی (`overflow-x:clip` + `min-width:0` سکشن‌ها).

## Files
- `css/app.css` (الحاق انتهایی)

## Acceptance (آینه دموی کامل)
- 1440/1024/768/640/390: ‏`scrollWidth<=innerWidth`، بدون اورلپ، منوها کامل باز، لاگ resize بومی، تب‌های مودال سالم.
- `git diff --check` تمیز.

## Type: implement
## Reference (شواهد تأیید در دموی git-ignored — spec بالا خودکفاست)
- الگوی رفتاری در `temp/hamnegar-demo` تأیید شده؛ پس از پیاده‌سازی، چک‌لیست Acceptance را در ریپو واقعی در ۵ عرض تکرار کن.
