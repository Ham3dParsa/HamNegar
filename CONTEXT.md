# CONTEXT.md — زبان همه‌جای هم‌نگار

واژه‌نامه دامنه. فقط معنا، بدون جزئیات پیاده‌سازی.

## ارائه‌دهنده (Provider)

سرویس بیرونی هوش مصنوعی که رونویسی یا پالیش را انجام می‌دهد.
شناسه‌های کانونیک: `groq | google | openrouter` به‌علاوه ارائه‌دهنده‌های
custom کاربر. `gemini` نام legacy همان `google` است؛ `zenspark` حذف‌شده و
هیچ معنای دامنه‌ای ندارد.

## زنجیره STT

فهرست مرتب ورودی‌های رونویسی. هر ورودی یک ارائه‌دهنده و یک مدل را مشخص می‌کند.

## زنجیره پالیش

فهرست مرتب ورودی‌های ویرایش متن. جدا از زنجیره STT است.

## Fallback

قاعده عبور: اگر ورودی فعلی با خطای قابل‌تلاش-بعدی (مثل 401/403/404/429)
شکست خورد، ورودی بعدی زنجیره امتحان می‌شود.

## ضبط (Recording)

چرخه عمر `idle | recording | sending | success | error` از فشردن دکمه تا
نمایش متن. لغو (Cancel) یعنی بازگشت به `idle` بدون درج متن.

## رونویسی (Transcription)

تبدیل صدا به متن خام از طریق یک ورودی زنجیره STT.

## پالیش (Polish)

ویرایش متن خام (نقطه‌گذاری، نیم‌فاصله، غلط‌گیری) از طریق زنجیره پالیش.
متن خام ورودی پالیش است، نه جایگزین آن.

## دیکشنری شخصی (Personal dictionary)

جایگزینی‌های لفظ‌به‌لفظ کاربر که بعد از پالیش روی متن اعمال می‌شود.

## سهمیه (Quota)

سقف مصرف روزانه هر ارائه‌دهنده در برابر مصرف ثبت‌شده.

## آمار مصرف (Usage)

رویدادهای ثبت‌شده رونویسی/پالیش که گزارش روزانه و کلی از آن ساخته می‌شود.

## موج (Wave)

پیکربندی نمایش موج صدا (نوع، رنگ، باند، پروفایل). حالت «پیروی از سراسری»
یعنی تب مقدار خودش را ندارد و از تنظیم سراسری استفاده می‌کند.

## Pill

پوسته شناور ضبط — همان چرخه ضبط با درج حبابی به‌جای درج در ویرایشگر اصلی.
